/** Lightweight, opt-in synthesized tactical feedback. No assets or autoplay required. */
export function createAudio(){let ctx:AudioContext|undefined;const beep=(hz:number,duration=.07)=>{ctx??=new AudioContext();const o=ctx.createOscillator(),g=ctx.createGain();o.frequency.value=hz;g.gain.setValueAtTime(.025,ctx.currentTime);g.gain.exponentialRampToValueAtTime(.0001,ctx.currentTime+duration);o.connect(g).connect(ctx.destination);o.start();o.stop(ctx.currentTime+duration)};return{click:()=>beep(420,.04),blast:()=>beep(90,.16),alert:()=>beep(760,.12),destroy:()=>ctx?.close()};}
